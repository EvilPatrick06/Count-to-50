# Count-to-50
This REPO contains a bash script that prints 1-50 into the terminal. This is done using an Until Loop. To run the bash script from your terminal, run: curl -sL https://raw.githubusercontent.com/EvilPatrick06/Count-to-50/main/Count_to_50.sh | bash
